# Music Social - API Gateway

## 🚀 Что сделано

Создан полнофункциональный **HTTP API Gateway** для микросервисной архитектуры Music Social с поддержкой:

- ✅ **gRPC-to-HTTP трансляция** - преобразование HTTP REST запросов в gRPC вызовы
- ✅ **JWT аутентификация** - полная система авторизации с access/refresh токенами
- ✅ **CORS поддержка** - готовность для фронтенд интеграции
- ✅ **Централизованная обработка ошибок** - унифицированные HTTP ответы
- ✅ **Health checks** - мониторинг состояния сервисов
- ✅ **Docker контейнеризация** - готовность к production

## 🏗️ Архитектура

```
Frontend/Mobile App
        ↓ HTTP/REST
   API Gateway (Go)
        ↓ gRPC  
    Users Service
        ↓
   PostgreSQL DB
```

## 📁 Структура проекта

```
services/
├── docker-compose.yml          # Оркестрация всех сервисов
├── gateway/                    # API Gateway
│   ├── main.go                # Основной код gateway
│   ├── Dockerfile             # Контейнеризация
│   ├── README.md             # Полная документация
│   ├── QUICK_START.md        # Быстрый старт
│   ├── API_EXAMPLES.md       # Примеры API вызовов
│   └── proto/                # gRPC proto файлы
├── users/                     # Users gRPC сервис
└── (другие сервисы...)       # Место для будущих сервисов
```

## 🔌 API Endpoints

### Публичные (без авторизации)
- `GET /__health` - Health check
- `POST /api/v1/auth/sign-up` - Регистрация пользователя
- `POST /api/v1/auth/sign-in` - Вход в систему
- `POST /api/v1/auth/refresh` - Обновление токена

### Защищенные (требуют JWT токен)
- `GET /api/v1/me` - Получение профиля пользователя
- `PUT /api/v1/me` - Обновление профиля
- `GET /api/v1/me/search-history` - История поиска
- `POST /api/v1/me/search-history` - Добавить в историю поиска
- `DELETE /api/v1/me/search-history` - Очистить историю поиска

## ⚡ Быстрый старт

### 1. Запустите все сервисы
```bash
cd services
docker compose up -d
```

### 2. Дождитесь готовности
```bash
docker compose ps
# Все сервисы должны показать статус "healthy"
```

### 3. Тестируйте API
```bash
# Health check
curl http://localhost:8080/__health

# Регистрация пользователя
curl -X POST http://localhost:8080/api/v1/auth/sign-up \
  -H "Content-Type: application/json" \
  -d '{
    "email": "test@example.com",
    "password": "password123",
    "username": "testuser", 
    "displayName": "Test User"
  }'
```

## 📖 Документация

- **[QUICK_START.md](services/gateway/QUICK_START.md)** - Быстрый старт и основные команды
- **[README.md](services/gateway/README.md)** - Полная документация по добавлению новых сервисов
- **[API_EXAMPLES.md](services/gateway/API_EXAMPLES.md)** - Примеры всех API вызовов с ответами

## 🔧 Что дальше?

### Для добавления нового микросервиса:

1. **Подготовьте gRPC сервис** с proto файлами
2. **Скопируйте proto файлы** в `gateway/proto/servicename/v1/`
3. **Добавьте gRPC клиент** в gateway код (см. README.md)
4. **Создайте HTTP обработчики** для новых endpoints
5. **Обновите docker-compose.yml** с новым сервисом
6. **Протестируйте** интеграцию

### Подробные инструкции в [services/gateway/README.md](services/gateway/README.md)

## 🌐 Порты и сервисы

- **API Gateway**: <http://localhost:8080>
- **PostgreSQL**: localhost:5432 (внутри Docker сети)
- **Users Service**: localhost:50051 (gRPC)

## 🛠️ Технологии

- **Go 1.23** - язык разработки gateway
- **gRPC** - межсервисное взаимодействие  
- **JWT** - авторизация (HS256)
- **PostgreSQL** - база данных
- **Docker & Docker Compose** - контейнеризация
- **Gorilla Mux** - HTTP роутинг
- **Protocol Buffers** - сериализация данных

---

**Статус**: ✅ Готово к использованию и расширению  
**Тестирование**: ✅ Все endpoints проверены и работают  
**Документация**: ✅ Полная документация создана